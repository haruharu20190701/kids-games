# こどもあそびば — プロジェクトメモ

## ねらい
子供（5歳前後）向けのゲーム・教育コンテンツを**少しずつ増やしていくコレクション**。
「軽い・安全・どの端末でも遊べる・できた！が気持ちいい」を大事にする。

## 現状（2026-06-23 時点）
- 公開中: https://haruharu20190701.github.io/kids-games/
- ゲーム **22本**（分類）:
  - 学び：✏️ひらがな（教科書体フォント同梱）・🔤ABC（speechSynthesisで英語発音）・🔢かずあそび・🧮けいさん（たし/ひき・テンキー）・🕐とけい
  - パズル/思考：🧩ぱずるぶろっく・🃏しんけいすいじゃく（生成カード絵）・🧩パズル（ジグソー・絵30枚×難易度3）・⭕まるばつ（1人/2人）
  - アクション：🐹もぐらたたき・🏒エアホッケー・🧱ブロックくずし・🧺キャッチ・🎣さかなつり・🗼つみきタワー・🏃はしってジャンプ・🚗3Dドライブ（three.js）
  - そうぞう：🎨おえかき（画像スタンプ・PNG保存）・🖍️ぬりえ・🎨いろまぜ（混色）・🎹ぴあの（おてほん9曲）
  - あつめる：🪲むしバトル（虫を集めてじゃんけんバトル・図鑑/localStorage保存）
- メニュー：`GAMES` 配列のデータ駆動。**全ゲームに生成アイコン画像**（読み込み失敗時 emoji）
- 各ゲーム：🏠ホーム・最小SEO・sitemap・縦横/タブレット対応・iOS保存（該当）・長押しメニュー抑制を完備
- **画像生成パイプライン**：PCのセルフホストランナー＋`imagegen-requests/request.txt` の push で自動生成（genspark）。キャラ・背景・アイコンを生成して各ゲームに同梱（フォールバックあり）

## これまでの学び
- **単一ファイル静的構成**は、配布・ホスティング・オフライン性で5歳向け小品に最適。
- **演出（音・揺れ・ほめ言葉）が「気持ちよさ」と継続意欲に直結**。地味な正解より派手な達成感。
- レスポンシブは**横向き（高さ制約）が要注意**。盤は `min(幅, 高さ)` で決め、回転時は状態を保ったまま組み直す。
- **CSSの `@media` 上書きはソース順**。基本ルールより後ろ（末尾）に置く。
- テストは**ローカルHTTP（`file://` 不可）＋複数画面サイズの実測**が有効。はみ出しと操作可否を数値で確認。
- 公開は **GitHub Pages が最短**。push するだけで反映。`.claude/` は gitignore で必ず除外。
- **保存は iOS が鬼門**：`<a download>` は iOS Safari で効かない。Web Share API（共有シート→写真に保存）→ダウンロード→長押し保存の3段で対応。
- **canvas は flex の中で巨大化する**：`min-width/height:0` を必ず付ける。座標は dpr とバッファ幅で写す。
- **素材は権利クリアなものだけ**：自作/生成AI/CC0のみ。第三者IP（公式ぬりえ等）は「印刷OK」でも転載しない。
- **ぬりえはレイヤー分離＋フラッドフィル**：線レイヤーを壁にバケツ塗り。画像線画は暗ピクセルを線として取り込む。
- **画像生成は slug単位で生成済み判定**＝同じslugは作り直されない（作り直しは別slug）。gensparkは色指定でも稀に線画→「FULLY COLORED / NOT a line drawing」を強めに。
- **生成中は main へ別pushしない**（ワークフローの push が競合で失敗）。生成画像の組み込みは `<img>`/`drawImage`＋emoji/図形フォールバック。
- **3Dは three.js を同梱**（CDN不可）。相対 import でオフライン維持。低ポリ＋霧＋追従カメラで軽量に。

## ロードマップ / 今後のアイデア
**新ゲーム**（✅=作成済み）
- ✅ひらがな ✅ABC ✅けいさん ✅とけい ✅もぐらたたき ✅いろまぜ ✅ブロックくずし ✅キャッチ
  ✅さかなつり ✅むしバトル ✅ジグソーパズル ✅まるばつ ✅つみきタワー ✅はしってジャンプ ✅3Dドライブ
- 未：めいろ、シューティング、3D迷路/的当て、リズム、もじ/かんじ書き取り など

**共通化**
- `shared/` に共通CSS（パレット/ボタン/猫）とヘルパJS（`beep`/`catReact`/canvasフィット）を切り出して各ゲームで再利用（現状は各ファイルに内蔵）

**メニュー / 体験**
- 最近あそんだゲーム、お気に入り、★獲得数の保存（localStorage。むしバトルで採用済み）
- 音量トグル、アクセシビリティ（文字サイズ・コントラスト）

**基盤**
- PWA化（manifest + service worker）でホーム追加・オフライン起動
- ワークフローのコミットを noreply@anthropic.com 名義にする等の整備（任意）

## 拡張のしかた
- 手順は `README.md`（利用者向け）と `CLAUDE.md`（規約・ハマりどころ）。
- 新規ゲームはスキル `/new-kids-game` で雛形手順を呼び出せる。
